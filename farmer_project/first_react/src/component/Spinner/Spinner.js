// Spinner.js
import React from 'react';
import './Spinner.css'; // Ensure to import the CSS

const Spinner = () => {
  return (
    <div className="spinner-overlay">
      <div className="spinner"></div>
    </div>
  );
}

export default Spinner;
